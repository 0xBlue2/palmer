[project]
name = "backend"
version = "0.1.0"
dependencies = [
    "fastapi",
    "fastapi-cli",
    "jinja2",
    "uvicorn",
    "python-multipart",
    "cohere"
]

[tool.fastapi]
entrypoint = "backend.main:app"
